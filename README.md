# 参考
https://core-tech.jp/blog/tech_log/4413/

# 起動
`docker compose up -d `

# 終了
`docker compose down`

# phpのサーバーに入る。(composerなどはここで実行)
`docker compose exec php-apache /bin/bash`

# ブラウザ
http://localhost:8080/
